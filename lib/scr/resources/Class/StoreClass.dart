class Store{
  String key,image,nameStore,address,district;
  String city,description,timeStart,timeEnd,token;

  Store(this.key, this.image, this.nameStore, this.address, this.district,
      this.city, this.description, this.timeStart, this.timeEnd,this.token);
}